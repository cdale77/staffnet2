<div class="row">
  <div class="col-md-12">
    <%= simple_form_for presenter.model,
                       url: resolve_duplicate_record_path(presenter.model),
                       method: :post, 
                       remote: true do |f| %>
      <div class="row">
        <div class="col-md-12">
          Primary record:<br/>
          <div class="col-md-6">
            <%= radio_button_tag :primary_record, 
                                  presenter.first_record.id, 
                                  true, class: "radio inline" %>
          </div>
          <div class="col-md-6">
            <%= radio_button_tag :primary_record, 
                                  presenter.second_record.id,
                                  false, class: "radio inline" %>
          </div>
        </div>
      </div>
      <div class="row">
        <div class="col-md-12">
          <div class="col-md-6">
            <h3>Record 1</h3>
            <%= render "record_fields", 
                      record_type: "#{presenter.first_record.id}",
                      record: presenter.first_record %>       
          </div>
          <div class="col-md-6">
            <h3>Record 2</h3>
            <%= render "record_fields", 
                      record_type: "#{presenter.second_record.id}",
                      record: presenter.second_record %> 
          </div>
        </div>
      </div>
      <div class="row">
        <div class="col-md-3 col-md-offset-3">
          <%= f.button :submit, 
                        "Merge", 
                        class: "btn btn-primary btn-ajax",
                        role: "button",
                        id: "btn-#{presenter.model.id}",
                        data: { "loading-text" => "Submitting ..."} %>
        </div>
      </div>
    <% end %>
  </div>
</div>
<hr />