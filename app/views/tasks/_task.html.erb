<tr>
  <td><%= task.name %></td>
  <td><%= task.project.name %></td>
  <td><%= task.hours %></td>
  <td><%= task.shift.date %></td>
  <td class="buttons"><%= link_to 'details', task_path(task), class: 'btn btn-mini', type: 'button' %>
    <%= link_to 'edit', edit_task_path(task), class: 'btn btn-mini', type: 'button' if admin_user? %></td>
</tr>