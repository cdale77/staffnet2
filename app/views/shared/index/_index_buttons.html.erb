<% begin Rails.application.routes.recognize_path(polymorphic_path(record)) %>
<%= link_to "details", polymorphic_path(record),
            class: "btn btn-xs btn-default", type: "button" %>
<% rescue %>
<% end %>
<% begin Rails.application.routes.recognize_path(edit_polymorphic_path(record)) %>
  <%= link_to "edit", edit_polymorphic_path(record),
            class: "btn btn-xs btn-default", type: "button" %>
<% rescue %>
<% end %>
